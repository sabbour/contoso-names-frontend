apiVersion: v1
kind: Service
metadata:
  name: contosonames-frontend
  namespace: contoso
spec:
  type: LoadBalancer
  selector:
    app: contosonames-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000